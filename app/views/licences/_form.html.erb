<%= form_with(model: licence, local: true) do |form| %>
  <% if licence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(licence.errors.count, "error") %> prohibited this licence from being saved:</h2>

      <ul>
      <% licence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :licence_type %>
    <%= form.text_field :licence_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
