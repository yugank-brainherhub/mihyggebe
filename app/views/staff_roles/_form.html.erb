<%= form_with(model: staff_role, local: true) do |form| %>
  <% if staff_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(staff_role.errors.count, "error") %> prohibited this staff_role from being saved:</h2>

      <ul>
      <% staff_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
