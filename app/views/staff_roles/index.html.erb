<p id="notice"><%= notice %></p>

<table class="responsive nowrap table table-hover">

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @staff_roles.each do |staff_role| %>
      <tr>
        <td><%= staff_role.name %></td>
        <td><%= link_to 'Show', staff_role %></td>
        <td><%= link_to 'Edit', edit_staff_role_path(staff_role) %></td>
        <td><%= link_to 'Destroy', staff_role, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Staff Role', new_staff_role_path %>
