<%= form_with(model: room_service, local: true) do |form| %>
  <% if room_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room_service.errors.count, "error") %> prohibited this room_service from being saved:</h2>

      <ul>
      <% room_service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :room_service_type_id %>
    <%= form.number_field :room_service_type_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
